export default function Hero() {
  return (
    <header className="hero">
      <p className="eyebrow">Smart Scaling Assistant</p>
      <h1>Guide Capacity Decisions with Clarity</h1>
      <p className="hero-copy">
        Estimate the next scaling move from pre-trained workload samples through one focused recommendation panel.
      </p>
      <div className="hero-indicators">
        <span className="hero-chip">Decision Engine Active</span>
        <span className="hero-chip">Sample-Based Inference</span>
        <span className="hero-chip">Pretrained Dataset Loaded</span>
      </div>
    </header>
  );
}
